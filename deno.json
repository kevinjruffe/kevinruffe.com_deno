{
  "tasks": {
    "build": "deno run --allow-write --allow-read --allow-env generator/generate.ts",
    "fmt": "deno fmt generator",
    "lint": "deno lint generator",
    "newPost": "deno run --allow-env --allow-write generator/newPost.ts",
    "serve": "npx http-server -c-1 blog/built"
  }
}
